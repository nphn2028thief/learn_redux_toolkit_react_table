const routes = {
    login: '/login',
    register: '/register',
    admin: '/admin',
    management: '/admin/stored',
    coursesManagement: '/admin/stored/courses',
    statistic: '/admin/stored/statistic',
};

const config = {
    routes,
};

export default config;
