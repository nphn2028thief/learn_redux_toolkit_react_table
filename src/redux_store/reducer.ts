import courseSlice from './course/course_slice';
import authSlice from './login/auth_slice';
import modalSlice from './modal_slice';

export const reducer = {
    courseSlice,
    authSlice,
    modalSlice,
};
